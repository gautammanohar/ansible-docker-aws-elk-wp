---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: metricbeatcontainer
      image: ubuntu
      image_version: trusty
      ansible_groups:
        - group1
verifier:
  name: testinfra
